/**
 * Tests to make sure objects work via dynamic and show proper warnings.
 */
const assert = require('assert');
const fs = require( "fs" );

describe('Semantics tests', () => {
  it( 'should keep track of persist objects', () => {
    assert.equal( true, false );
  });

  it( 'should acknowlage script changes and contain new methods and remove old methods', () => {
    assert.equal( true, false );
  });

  it( 'should acknowlage method changes and reflect new method code for old method pointers', () => {
    assert.equal( true, false );
  });

  it( 'should get/set data from objects that have changed using new object abilities', () => {
    assert.equal( true, false );
  });

  it( 'should follow a dependency tree when a file changed and the inheritance chain should reflect that', () => {
    assert.equal( true, false );
  });
} );
